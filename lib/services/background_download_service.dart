// This file is intentionally empty.
// The flutter_background_service plugin has been removed.
// flutter_gemma (v0.13+) handles Android foreground service for large
// model downloads (>500 MB) natively via fromNetwork(foreground: true).
// No separate background service plugin is needed.
